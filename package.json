{
  "name": "zootorial",
  "version": "0.0.3",

  "directories": {
    "lib": "./lib"
  },

  "main": "./lib/zootorial",

  "devDependencies": {
    "coffee-script": "1.3.x",
    "silver-server": "0.0.x",
    "stylus": "0.29.x"
  },

  "scripts": {
    "prepublish": "coffee --compile lib/*.coffee && stylus css/*.styl"
  }
}
